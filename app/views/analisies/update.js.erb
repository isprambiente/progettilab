<% if @analisy.errors.blank? %>
  if ( $('html').hasClass('is-reveal-open') == true ) {
    $('#myModal').foundation('close');
  }else{
    $('#analisy').html("<%= escape_javascript( render 'edit' ) %>");
    alert('Modifica avvenuta con successo!');
  }
<% else %>
  if ( $('html').hasClass('is-reveal-open') == true ) {
    $('#modal-body').html("<%= escape_javascript( render 'form' ) %>");
  }else{
    $('#analisy').html("<%= escape_javascript( render 'edit' ) %>");
    alert("La modifica non è avvenuta a causa di <%= @analisy.errors.count %> errori!");
  }
<% end %>