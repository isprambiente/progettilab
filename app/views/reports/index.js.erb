<% if @modal %>
  $('#modal-body').html("<%= escape_javascript( render @section, job: @job, reports: @reports ) %>");
<% else %>
  $('#reports').html("<%= escape_javascript( render @section, job: @job, reports: @reports ) %>");
<% end %>
reports_table();